function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background("rgb(245,234,247)");

  textAlign(CENTER, CENTER);
  textSize(24);
  fill("pink");
  text("hello", 50, 50);
}
